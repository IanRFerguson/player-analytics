# GCP Service Accounts

* This repo should contain `load` and `transform` service account files (or one account file with dual permissions)
* Your local `development.env` file should point to the files in this subdirectory